The "My name is Betty" script switches the current user to the use
r betty.                                                          

The "Who am I" script prints the effective username of the current
user.                                                             

The "Groups" script prints all the groups the current user is part
 of.                                                              

The "New ower" script changes the owner of the script hello to the
 user betty.                                                      

The "Empty" script creates an empty file called hello. 

The "Execute" script adds execute permission to the owner of the file hello.

The "Multiple permissions" script adds execute permission to the owner and the group owner, and read permission to other users, to the file hello.

The "Everybody!" script adds execution permission to the owner, the group owner and the other users, to the file hello.

The "James Bond" script sets no permissions at all to the owner and the group owner, and all permissions to other users, to the file hello

"The "John Doe" script sets the mode of the file hello 

The "Look in the mirror" script sets the mode of the file hello to the same as the olleh's mode.

The "Directories" script adds execute permission to all subdirectories of the current directory for the ower, the group owner and all other users without changing the permissions of regular files.

The "More directories" script creates a directory called my_dir with permissions 751 in the working directory

The Change group" script changes the group owner to school for the file hello.
