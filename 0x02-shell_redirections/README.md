The "Hello World" script prints “Hello, World”, followed by a new line to the standard output.

The "Confused smiley" script displays a confused smiley "(Ôo)'.

The "Let's display a file" script displays the content of the /etc/passwd file.

The "What about 2" script displays he content of /etc/passwd and /etc/hosts.

The "Last lines of a file" script displays the last 10 lines of /etc/passwd.

The "I'd prefer the first ones actually" script displays the first 10 lines of /etc/passwd.

The "Line 2" for task 6 displays the third line of the file iacta.

The "It is a good file that cuts iron without making a noise" script creates a file named exactly \*\\'"Best School"\'\\*$\?\*\*\*\*\*:) containing the text Best School ending by a new line.

The "Save current state of directory" script writes into the file ls_cwd_content the result of the command ls -la. If the file ls_cwd_content already exists, it should be overwritten. If the file ls_cwd_content does not exist, create it.

The "Duplicate last line" script duplicates the last line of the file iacta.

The "No more javascript" script deletes all the regular files (not the directories) with a .js extension that are present in the current directory and all its subfolders.

The "Don't just count your directories, make your directories count" script counts the number of directories and sub-directories in the current directory.

The "What’s new" script displays the 10 newest files in the current directory.

The "Being unique is better than being perfect" script takes a list of words as input and prints only words that appear exactly once.

The "It must be in that file"   script displays lines containing the pattern “root” from the file /etc/passwd.

The `"Count that word" script displays the number of lines that contain the pattern “bin” in the file /etc/passwd.

The "What's next?" script displays lines containing the pattern “root” and 3 lines after them in the file /etc/passwd.

The " I hate bins" script displays all the lines in the file /etc/passwd that do not contain the pattern “bin”.

The " Letters only please" script displays all lines of the file /etc/ssh/sshd_config starting with a letter.

The "A to Z" script replaces all characters A and c from input to Z and e respectively.

The "Without C, you would live in hiago" sctript removes all letters c and C from input.

The "esreveR" script reverse its input.

The "DJ Cut Killer" script displays all users and their home directories, sorted by users.  
