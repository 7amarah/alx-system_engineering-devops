The "<o>" script creates an alias for the "ls" command with the value of "rm *".

The "Hello you" script prints "hello user", where user is the Linux user.

The "The path to success is to take massive, determined action" script adds /action to the PATH, where it is the last directory that the shell looks into when looking for a program.

The "If the path be beautiful, let us not ask where it leads" script counts the number of directories in the PATH.

The "Global variables" script lists environment variables.

The "Local variables" script lists all local variables and environment variable, and functions.

The "Local variable" script creates a new local variable with the name "BEST"  and a value of "School".

The "Globale variable" script creates a new global variable with the name "BEST" and the value "School".

The "Every addition to true knowledge is an addition to human power" script prints the result of the addition of 128 with the value stored in the environment variable "TRUEKNOWLEDGE", folowed by a new line.

The "Divide and rule" script prints the result of the variable "POWER" divided by the variable "DIVIDE", followed by a new line.

The "Love is anterior to life, posterior to death, initial to creation, and the exponent of breath" script displays the result of "BREATH" to the power of "LOVE"  which are both environment variables, followed by a new line.

The " There are 10 types of people in the world -- Those who understand binary, and those who don't" script converts a number from base 2 which is stored in the environment variable BINARY to base 10.

The "Combination" script prints all possible combinations of te=wo letters, except oo; with letters in lower case, from a to z, and one combinantion per line and a maximum of 64 charcters.

The "Floats" script prints a number which will be stored in the environment variable NUM with two decimal places, followed by a new line.
